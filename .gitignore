/Debug
/Release

# Products of build.sh and run.sh.
/build/
/stage/
/neogeotest.tos
/RUNNER.PRG
/*.gen.asm
/*.gen.ini
__pycache__/

# ROM sets staged into <game>/ by install_roms.py, the BIOS that sits beside
# the program, and the tile caches the emulator writes next to the ROMs.
/sp-s2.sp1
/sfix.sfi
/*/*.p1
/*/*.p2
/*/*.ep1
/*/*.sp2
/*/*.c[1-8]
/*/*.rom
/*/*.bin
/*/*.tls
/*/*.ubm

# The author's distribution archive, staged from by install_roms.py.
/neogeo_full.zip
